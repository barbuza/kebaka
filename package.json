{
  "name": "kebaka",
  "version": "0.1.0",
  "description": "immutable firebase wrapper",
  "main": "index.js",
  "scripts": {
    "prepublish": "npm run build && npm test",
    "build": "tsc && babel -d lib es6",
    "test": "nyc -i babel-register -i babel-polyfill tape tests/*.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-polyfill": "^6.5.0",
    "babel-preset-es2015": "^6.5.0",
    "babel-register": "^6.5.2",
    "nyc": "^5.6.0",
    "tape": "^4.4.0",
    "ts-node": "^0.5.5",
    "typescript": "^1.8.2"
  },
  "dependencies": {
    "asap": "^2.0.3",
    "immutable": "^3.7.6"
  }
}
